export function renderReadingsPage(container: HTMLElement) {
    container.innerHTML = `
      <section class="readings-section">
        <h2>PSYCHIC READING SERVICES</h2>
        <div class="reading-options">
          <!-- Add your reading options here -->
        </div>
      </section>
    `;
  }